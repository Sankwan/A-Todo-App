import 'package:flutter/material.dart';

class Businesss extends StatelessWidget {
  const Businesss({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}